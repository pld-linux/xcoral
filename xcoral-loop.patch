--- color_area.c
+++ color_area.c	1998/10/08 14:59:59
@@ -750,8 +750,9 @@
 	pos = ie_beginning_of_line(text) + text -> page.hs_delta;
 
 	if (ie_the_char(text,pos) != *s) {
+	  int catch = 0;
 	  /* on est apres un tab, il faut donc se placer apres */
-	  while (ie_the_char(text,pos) != '\t') {
+	  while ( (catch=ie_the_char(text,pos)) != '\t' && catch != 0 ) {
 	    pos++;
 	  }
 	}
